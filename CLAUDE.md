# my-blog

마크다운(.md) 파일을 읽어서 정적 블로그 웹사이트로 변환하는 프로젝트.

## 기술 제약

- **프레임워크 없음.** React, Vue, Svelte 등 사용 금지. 순수 HTML, CSS, JavaScript(vanilla)로만 구현한다.
- 번들러/빌드 툴체인(Webpack, Vite 등)도 넣지 않는다. 브라우저에서 그대로 열리거나, 정적 파일 서버 하나로 서빙 가능한 구조를 유지한다.
- 마크다운 파싱은 CDN으로 불러온 marked.js를 사용한다(직접 파서 구현 안 함).

## 콘텐츠 파이프라인 (확정)

- **빌드 단계 없음.** 브라우저가 런타임에 `fetch`로 `/posts/*.md`를 읽어 marked.js로 그 자리에서 HTML로 렌더링한다. Node 빌드 스크립트, 정적 HTML 사전 생성 방식은 사용하지 않는다.
- `posts/manifest.json`이 "어떤 글이 존재하는가"의 단일 소스다. 브라우저에서는 디렉터리 목록을 읽을 수 없으므로, 새 글을 추가할 때마다 `.md` 파일과 함께 이 manifest에 `slug`/`title`/`date`/`excerpt`/`tags` 항목을 수동으로 추가해야 한다. `slug`는 파일명(확장자 제외)과 반드시 일치해야 한다.
- `index.html`은 목록/홈, `post.html?slug=<slug>`는 상세 페이지다. `post.js`가 쿼리스트링의 `slug`로 manifest를 조회하고 `posts/<slug>.md`를 fetch한다.
- `file://`로 직접 열면 `fetch`가 CORS로 막히므로, 로컬에서 볼 때는 반드시 정적 서버(`python -m http.server` 등)로 열어야 한다. 자세한 내용은 [README.md](README.md) 참고.
- 스크립트는 ES 모듈(`<script type="module">`)로 작성한다. 공용 로직은 `js/markdown.js`(마크다운 렌더링), `js/manifest.js`(manifest fetch/캐시), `js/mascot.js`(토끼 SVG 생성/애니메이션 트리거)로 분리되어 있고, `js/home.js`/`js/post.js`가 각 페이지에서 이를 조합한다.

## 디자인 방향

전체적으로 **세련되고 깔끔하며 읽기 편한** 블로그. 화려함보다 가독성이 우선이다.

### 컬러

- 배경은 **핑크 계열의 다양한 채도**로 구성한다. 예시 팔레트:
  - 배경 베이스: 아주 옅은 핑크 (`#FFF5F7` ~ `#FFE9EF` 근처)
  - 섹션 구분용 중간 톤 핑크 (`#FFD6E4`, `#FFB6C9`)
  - 포인트/CTA용 진한 핑크 (`#FF6B9D`, `#E85D9C`)
  - 텍스트는 핑크 배경 위에서도 대비가 확실한 어두운 톤(짙은 자주/차콜 계열, 예: `#3D2C35`)을 사용해 가독성을 지킨다.
- **라이트 모드만 지원**한다 (다크 모드 요구 없음).

### 마스코트: 토끼

- 귀여운 **토끼 일러스트레이션**이 사이트 곳곳에서 행동하거나(책 읽기, 안내하기, 손짓하기 등) 지시하는(스크롤 유도, 섹션 안내 등) 역할을 한다.
- 토끼는 정적 이미지가 아니라 **움직이거나 동작하는** 요소여야 한다 — CSS 애니메이션(귀 흔들기, 깡총거리기, 손짓) 또는 인라인 SVG + CSS/JS 애니메이션으로 구현한다.
- 외부 아이콘 팩 의존 없이 SVG로 직접 그리는 것을 기본으로 한다.

### 반응형

- **모바일에서 잘 보이는 것**이 필수 조건. 모바일 우선으로 레이아웃을 잡고, 브레이크포인트를 넉넉히 테스트한다.
- 터치 타겟 크기, 폰트 크기, 여백을 모바일 화면에서 직접 확인한다.

## 작업 시 유의사항

- UI 변경 시 반드시 정적 서버로 브라우저에서 실제로 열어 확인한다 (모바일 뷰포트 포함).
- 토끼 마스코트를 새로 추가/변경할 때는 `js/mascot.js`의 공유 SVG 템플릿과 `css/mascot.css`의 포즈별 키프레임을 함께 확인한다. 새 포즈는 `rabbit--<pose>` 클래스 규칙을 따른다.
